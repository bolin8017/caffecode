// Re-export from shared — single source of truth for problem selection
export { selectProblemForUser, type ProblemSelectionContext } from '@caffecode/shared'
export type { SelectedProblem } from '@caffecode/shared'
